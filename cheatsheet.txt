# FreeBSD Cheat Sheet

## Tips

### Search in manual pages names and descriptions, or as fulltext

    man -k wifi
    man -K wifi_fulltext

### Live CD tips

Start with re-mount of the root filesystem for read-write:

    mount -u -o rw /

## Wifi connection

### List

    ifconfig wlan0 scan

### Prepare rc.conf if needed

### Connect

    wpa_passphrase "SSID" "PASSWORD" > /etc/wpa_supplicant.conf
    ifconfig wlan0 up
    wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant.conf
    dhclient wlan0

## Mount filesystems

### Mount ZFS disk partition from external or internal drive

List and mount:

    zpool import
    zpool import -o altroot=/mnt POOLNAME

### Mount UNKNOWN disk partition

Detect filesystem, mount and unmount:

    fstyp /dev/da0p1
    mount -t ufs /dev/da0p1 /mnt/disk
    umount /mnt/disk

## Disk partitioning

### Create partition scheme on an external disk

Exact example of default FreeBSD partitioning scheme during system installation.

Show disks and partitions:

    gpart show
    geom disk list

Destroy and then create new GPT partition table:

    gpart destroy -F da0
    gpart create -s gpt da0

Create EFI, FreeBSD boot, swap:

    gpart add -t efi -s 260M da0
    gpart add -t freebsd-boot -s 512K da0
    gpart add -t freebsd-swap -s 4G da0

Create ZFS partition *without* `-s`, so it will stretch to the end of the disk:

    gpart add -t freebsd-zfs da0

Format EFI and ZFS partition. Boot and swap do not have to be formatted:

    newfs_msdos -F 32 -c 1 /dev/da0p1
    zpool create mypool /dev/da0p4

Check result:

    gpart show da0
    zpool status
    zfs list

## Backup and restore with ZFS root partition

### Backup and restore partition table, `/efi` and `/boot` partitions

Backup GPT, `/efi` and `/boot`:

    gpart backup da0 > /mnt/backup_gpart_backup_partition_scheme.backup
    dd if=/dev/da0p2 of=/mnt/backup_freebsd_boot.img bs=512 status=progress
    tar -C /mnt/efi -cf /mnt/backup_efi_partition.tar .

Restore GPT, `/efi` and `/boot`:

    gpart restore da0 < /mnt/backup_gpart_backup_partition_scheme.backup
    tar -C /mnt/efi -xf /mnt/efi-partition.tar
    dd if=/mnt/da0p2-freebsd-boot.img of=/dev/da0p2 bs=512

### Backup and recover root partition with ZFS

Create a ZFS backup snapshot:

    zfs snapshot zroot/ROOT/default@BEFORE_UPGRADE_TO_V15.0

Create new boot environment from the ZFS snapshot:

    bectl create -e zroot/ROOT/default@BEFORE_UPGRADE_TO_V15.0 before-v15

Suppose here you are doing update of FreeBSD and it FAILED:

    freebsd-update -r 15.0-RELEASE upgrade
    FATAL ERROR

Recover by setting active boot environment for the next reboot:

    bectl activate -t before-v15
    reboot

### Backup and recover root partition from external drive

Send snapshot to external disk:

    zfs send zroot/ROOT/default@BEFORE_UPGRADE_TO_V15.0 > /mnt/zroot--ROOT--default@BEFORE_UPGRADE_TO_V15.0.zfs

Receive snapshot from external disk to new default:

    zfs receive zroot/ROOT/new_default < /mnt/zroot--ROOT--default@BEFORE_UPGRADE_TO_V15.0.zfs

Activate new boot environment for next reboot:

    bectl activate new_default
    reboot
